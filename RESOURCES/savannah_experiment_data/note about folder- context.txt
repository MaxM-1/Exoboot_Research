This folder contains two .csv files that come from an experiment ran by another person in my research lab (savannah) in this experiment the EXACT same Dephy Exoboot was used but it was controlled by a controller that was premade by Dephy and loaded onto the Exoboot - thus it was not controlled via python but through the Dephy GUI - the experiment that was performed was different than the one that I am running but both Exoboots ran very well during the experiment (no jittering, good heel strike detection, no weird issues). I have attached the .csv files for the whole experiment maybe some insights can be gathered from this. 

NOTE: the columns I believe are a little different (more or less) than the columns that are output from the python controller and this is normal as Dephy's preloaded controller that is controlled by the GUI outputs slightly different columns but the columns that are consistent across the python controller generated .csv and the .csv's here are assumed to be the same. 